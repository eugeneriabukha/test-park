from KeywordDriver import Options
# Test case to check the operation of Options Class
# Create Object for Options Class and returns a Dict with Preference as the value
b=Options("DirectInput")
print b
